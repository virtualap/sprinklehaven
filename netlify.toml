[build]
  # Build command
  command = "npm run build"

  # Directory to deploy (default for Create React App)
  publish = "build"

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
